'use strict';

var errorMessage_fn = ['$scope', 'Page', 'restAPI', '$modal', '$rootScope', '$state',
	function($scope, Page, restAPI, $modal, $rootScope, $state) {
		var vm = $scope;



	}
];

module.exports = angular.module('app.pactlAssist.errorMessage', []).controller('errorMessageCtrl', errorMessage_fn);